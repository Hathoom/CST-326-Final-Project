using UnityEngine;

namespace GauravPowerUps.Scripts
{
    public class DoubleDamagePU : MonoBehaviour
    {
        public GameObject bulletPrefab;
        // Start is called before the first frame update
        void Start()
        {
            // var = bulletPrefab.GetComponent<scriptName>;
        }

        // Update is called once per frame
        void Update()
        {
            // Assuming bulletPrefab has a script with a damage value
            // scriptName.damage *= 2
        }
    }
}
